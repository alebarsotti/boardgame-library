{
  "name": "boardgame-library",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:visual": "playwright test tests/visual-refresh.spec.js",
    "test:visual:headed": "playwright test --headed",
    "test:visual:capture": "playwright test tests/visual-capture.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
